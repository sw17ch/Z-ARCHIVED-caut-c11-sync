{{#specInfo}}
{{#aiInfo}}
CC=clang
CFLAGS=-O3 -Wall -Wextra -std=c11 -pedantic
PROG=run_tests

CFILES := cauterize.c {{cLibName}}.c {{cLibName}}_ai.c {{cLibName}}_assertions.c socket99.c
HFILES := cauterize.h {{cLibName}}.h {{cLibName}}_ai.h socket99.h greatest.h

all: $(PROG)
clean:
	rm $(PROG)

$(PROG): $(HFILES) $(CFILES)
	$(CC) $(CFLAGS) $(CFILES) -o $@
{{/aiInfo}}
{{/specInfo}}
